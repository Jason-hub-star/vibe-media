export const publicPatterns = [
  "bold shell with readable editorial panels",
  "icon-driven navigation and utility markers",
  "large banner slot plus compact supporting strips"
] as const;

export const adminPatterns = [
  "dense but readable status boards",
  "high-contrast state chips",
  "clear action hierarchy without nested cards"
] as const;
